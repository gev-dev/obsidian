# 🚀 Пособие для Юного Инженера: Создание «Тактильного метронома для терапии болезни Паркинсона»

---

## 🎯 Что мы будем делать?

Представь, что ты можешь помочь людям с болезнью Паркинсона ходить увереннее и плавнее! Просто с помощью умного браслета, который подсказывает ритм вибрацией. Невероятно, правда?

Мы создадим **умный браслет-метроном**, который:

- 📳 Вибрирует с заданным ритмом (как метроном, только тактильный)
- ⚙️ Позволяет регулировать темп вибрации (быстрее или медленнее)
- 🔋 Работает автономно без проводов
- 🎚️ Имеет кнопки управления прямо на браслете

Это устройство помогает людям с болезнью Паркинсона синхронизировать свои движения с ритмом, что улучшает походку и снижает тремор. Ты станешь настоящим биомедицинским инженером!


---

## 🛠️ Что нам понадобится?

### Электронные компоненты:

1. **Arduino Nano** или **Arduino Pro Mini** - Это мозг нашего устройства!
2. **Вибромотор** (1-2 штуки) - Создаёт тактильные импульсы (можно взять из старого телефона или купить готовый)
3. **MOSFET IRF840 (1 штука) - Для управления мотором
4. **Резистор 1 кОм** (1 штука) - Для защиты транзистора
5. **Резистор 220 Ом** (на затвор MOSFET)
6. **Диод 1N4007** (1 штука) - Защита от обратного тока
7. **Потенциометр 10 кОм** (для регулировки темпа)
8. **Тактовая кнопка** - СТАРТ/СТОП
9. **OLED-дисплей 0.96"** 128×64 (I2C, адрес 0x3C)
10. **Аккумулятор Li-Po 3.7V 500-1000 mAh** с преобразователем 3.7V→5V (рекомендуется)
11. **Провода-перемычки** (10-15 штук)
12. **Макетная плата** или печатная плата для пайки

### Материалы для корпуса:

- ⌚ Широкая резинка на липучке
- 📦 Небольшая пластиковая коробочка (5×3×2 см) или 3D-печатный корпус
- 🔧 Липучки или двусторонний скотч
- ✂️ Ножницы
- 🔌 Термоклей или изолента
- 🧵 Нитки и иголка (для пришивания корпуса к браслету)

### Программное обеспечение:

- 💻 Arduino IDE 

**Не забудь установить библиотеки для дисплея:**
1. Открой Arduino IDE
2. Перейди в **Скетч → Подключить библиотеку → Управление библиотеками**
3. Найди и установи **Adafruit SSD1306**
4. Найди и установи **Adafruit GFX Library**

---

## 🤔 Как это работает?

### Принцип работы устройства:

**Тактильная стимуляция вместо звуковой:**

Обычный метроном издаёт звуковые сигналы: "тик-так-тик-так". Наш браслет делает то же самое, но вместо звука использует **вибрацию**! Это удобно, потому что:

- Не нужны наушники
- Никто вокруг не слышит
- Работает даже в шумных местах
- Ощущается более естественно
### Почему это работает?

1. **Внешний ритм помогает мозгу** — при Паркинсоне нарушается внутренний "таймер" движений, а внешний ритм компенсирует это
2. **Тактильные сигналы эффективнее звуковых** — кожа воспринимает вибрацию напрямую, минуя перегруженные слуховые центры
3. **Эффект сохраняется после тренировки** — регулярное использование улучшает походку даже без устройства
4. **Снижает риск падений** — более ровная походка = лучший баланс

### Научные исследования:

- 📊 Исследования показывают улучшение походки на **20-30%** при использовании ритмической стимуляции
- 🧠 МРТ показывает, что ритм активирует двигательные зоны мозга, обходя повреждённые участки
- ⏱️ Эффект заметен уже через **2-3 недели** регулярного использования
### Как устройство помогает при болезни Паркинсона:

1. **Мозг получает ритмичные сигналы** через кожу
2. **Человек синхронизирует шаги** с этими импульсами
3. **Походка становится более плавной** и уверенной
4. **Тремор (дрожание) снижается** благодаря ритмической стимуляции

### Электронная схема работает так:

1. **Arduino** генерирует импульсы с заданной частотой
2. **Транзистор** усиливает сигнал для мотора
3. **Вибромотор** создаёт короткую вибрацию
4. **Потенциометр** позволяет менять темп прямо на ходу
5. **Аккумулятор** питает всю систему несколько часов
6. **Экран**  показывает текущий темп и состояние устройства

---

## 🔨 Пошаговая сборка

### Шаг 1: Подготовка Arduino (15 минут)

1. Достань Arduino Nano из упаковки
2. Подключи его к компьютеру USB-кабелем
3. Открой Arduino IDE и выбери свою плату в меню: **Инструменты → Плата → Arduino Nano**
4. Выбери процессор: **Инструменты → Процессор → ATmega328P (Old Bootloader)**

✅ **Проверка:** На Arduino должен загореться красный светодиод питания!

---

### Шаг 2: Сборка схемы на макетной плате (40 минут)

**Схема подключения вибромотора через транзистор:**

```
КОМПОНЕНТ             →    ПОДКЛЮЧЕНИЕ

Вибромотор (+) -> 5V
Вибромотор (-) -> MOSFET DRain (D)
MOSFET Source (S) -> GND
MOSFET Gate (G) -> Arduino D9 через резистор 220Ω
Параллельно мотору — диод 1N4007 (анод к минусу мотора, катод к плюсу)

Резистор 100kΩ: от Gate к GND (чтобы MOSFET был закрыт при выключенном Arduino)
```

**Схема подключения кнопок:**

```

КНОПКА           →    ПОДКЛЮЧЕНИЕ

Кнопка СТАРТ     →    D2 Arduino → один контакт, другой контакт → GND
Потенциометр (средний контакт) -> A0 (Arduino)
Потенциометр (крайние контакты) -> 5V и GND
```

**Схема питания:**

```
КОМПОНЕНТ        →    ПОДКЛЮЧЕНИЕ

Аккумулятор +    →    Выключатель → VIN Arduino
Аккумулятор -    →    GND Arduino
Модуль TP4056    →    Подключить между аккумулятором и Arduino
```


💡 **Важно про диод:** Диод защищает Arduino от обратного тока мотора. Подключай его **параллельно мотору**, полоской (катод) к минусу мотора.

✅ **Проверка:** Все провода должны сидеть плотно, ничего не болтается!

---

### Шаг 3: Загрузка базовой программы и тестирование (20 минут)

Сначала загрузим простой код, чтобы проверить, работает ли мотор:

```cpp
// Простой тест вибромотора

const int MOTOR_PIN = 9;  // PWM пин для мотора

void setup() {
  pinMode(MOTOR_PIN, OUTPUT);
  Serial.begin(9600);
  Serial.println("Motor test started!");
}

void loop() {
  // Включаем мотор на 200 мс
  digitalWrite(MOTOR_PIN, HIGH);
  Serial.println("Motor ON");
  delay(200);
  
  // Выключаем на 800 мс
  digitalWrite(MOTOR_PIN, LOW);
  Serial.println("Motor OFF");
  delay(800);
}
```

**Что должно произойти:**

- Мотор вибрирует 1 раз в секунду
- В Serial Monitor видны сообщения "Motor ON" и "Motor OFF"

💡 **Если мотор не работает:**

- Проверь подключение транзистора (правильно ли определены ножки)
- Проверь, подключён ли резистор между Arduino и базой транзистора
- Убедись, что диод подключён правильно (полоской к минусу мотора)

---

### Шаг 4: Калибровка частоты вибрации (15 минут)

Теперь нужно подобрать удобную частоту вибрации. Для терапии Паркинсона обычно используют ритм **60-120 ударов в минуту** (как темп ходьбы).

Загрузи этот код для настройки частоты:

```cpp
// Калибровка частоты метронома

const int MOTOR_PIN = 9;
const int VIBRATION_DURATION = 100;  // Длительность вибрации в мс

// Частота в BPM (beats per minute - ударов в минуту)
// Стандартные значения для ходьбы: 60-120 BPM
int bpm = 90;  // Начальная частота

void setup() {
  pinMode(MOTOR_PIN, OUTPUT);
  Serial.begin(9600);
  Serial.println("=== METRONOME CALIBRATION ===");
  Serial.print("Current BPM: ");
  Serial.println(bpm);
  Serial.println("Send '+' to increase, '-' to decrease");
}

void loop() {
  // Проверяем Serial для изменения частоты
  if (Serial.available() > 0) {
    char command = Serial.read();
    if (command == '+' && bpm < 180) {
      bpm += 5;
      Serial.print("BPM increased to: ");
      Serial.println(bpm);
    } else if (command == '-' && bpm > 30) {
      bpm -= 5;
      Serial.print("BPM decreased to: ");
      Serial.println(bpm);
    }
  }
  
  // Вибрация
  digitalWrite(MOTOR_PIN, HIGH);
  delay(VIBRATION_DURATION);
  digitalWrite(MOTOR_PIN, LOW);
  
  // Рассчитываем паузу для нужной частоты
  int interval = (60000 / bpm) - VIBRATION_DURATION;
  delay(interval);
}
```

**Как настроить частоту:**

1. Загрузи код на Arduino
2. Открой Serial Monitor (Инструменты → Монитор порта)
3. Надень браслет на руку
4. Попробуй ходить под ритм вибрации
5. Если слишком быстро — отправь знак **-** в Serial Monitor
6. Если слишком медленно — отправь знак **+**
7. Найди комфортную частоту и **запиши это значение BPM** (например, 85)

💡 **Рекомендации по частоте:**

- **60-70 BPM** — медленная прогулка
- **80-90 BPM** — обычная ходьба
- **100-120 BPM** — быстрая ходьба
- **120+ BPM** — лёгкий бег

---
### Шаг 5: Проверка экрана (20 мин)

📋 **Как проверить модуль модуль**

1. **Загрузи тестовый код** на Arduino
2. **Подключи OLED дисплей** по схеме:
    - SDA → A4
    - SCL → A5
    - VCC → 5V
    - GND → GND
3. **Открой Serial Monitor** (9600 бод)
4. **Наблюдай результат:**

✅ **Если всё работает:*
- На экране появятся смешные надписи
- В Serial Monitor будет "✅ OLED дисплей работает!"
- Будет работать простая анимация

❌ **Если есть проблемы:**
- В Serial Monitor будут подробные инструкции
- Программа остановится с сообщением об ошибке

```cpp
// ===================================
// ТЕСТ OLED ДИСПЛЕЯ
// Простая проверка работы экрана
// ===================================

#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET -1
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

void setup() {
  Serial.begin(9600);
  Serial.println("=== ТЕСТ OLED ДИСПЛЕЯ ===");
  
  // Инициализация дисплея
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) {
    Serial.println(F("❌ OLED дисплей не найден!"));
    Serial.println(F("Проверь подключение:"));
    Serial.println(F("- SDA -> A4"));
    Serial.println(F("- SCL -> A5"));
    Serial.println(F("- VCC -> 5V"));
    Serial.println(F("- GND -> GND"));
    while(1); // Останавливаем программу
  }
  
  Serial.println(F("✅ OLED дисплей работает!"));
  
  // Очистка экрана
  display.clearDisplay();
  
  // Выводим смешные фразы по очереди
  display.setTextSize(2);
  display.setTextColor(SSD1306_WHITE);
  
  // Первая фраза
  display.setCursor(10, 0);
  display.println("Привет,");
  display.setCursor(20, 20);
  display.println("Юный");
  display.setCursor(10, 40);
  display.println("Инженер!");
  display.display();
  
  delay(3000);
  
  // Вторая фраза
  display.clearDisplay();
  display.setCursor(5, 10);
  display.println("Мой экран");
  display.setCursor(15, 30);
  display.println("работает!");
  display.setCursor(5, 50);
  display.println("Ура! 🎉");
  display.display();
  
  delay(3000);
  
  // Третья фраза с анимацией
  display.clearDisplay();
  display.setCursor(0, 0);
  display.println("Проверка");
  display.setCursor(0, 20);
  display.println("пикселей:");
  
  // Рисуем прогресс-бар
  for(int i = 0; i < 128; i++) {
    display.drawLine(i, 45, i, 55, SSD1306_WHITE);
    display.display();
    delay(20);
  }
  
  display.setCursor(30, 55);
  display.println("Готово!");
  display.display();
}

void loop() {
  // Простая анимация для демонстрации работы
  static int counter = 0;
  
  display.clearDisplay();
  display.setTextSize(1);
  display.setCursor(0, 0);
  display.println("=== ТЕСТ ДИСПЛЕЯ ===");
  
  display.setTextSize(2);
  display.setCursor(20, 20);
  display.print("Счет: ");
  display.print(counter);
  
  // Рисуем смайлик
  display.drawCircle(64, 45, 10, SSD1306_WHITE); // голова
  display.drawPixel(60, 43, SSD1306_WHITE); // левый глаз
  display.drawPixel(68, 43, SSD1306_WHITE); // правый глаз
  
  // Рот (меняется в зависимости от счетчика)
  if(counter % 4 == 0) {
    display.drawPixel(64, 48, SSD1306_WHITE); // нейтральный
  } else if(counter % 4 == 1) {
    display.drawLine(62, 48, 66, 48, SSD1306_WHITE); // улыбка
  } else if(counter % 4 == 2) {
    display.drawPixel(64, 48, SSD1306_WHITE); // нейтральный
  } else {
    display.drawLine(62, 49, 66, 49, SSD1306_WHITE); // грусть
  }
  
  display.display();
  
  counter++;
  delay(500);
}

```


---
### Шаг 6: Загрузка основной программы  (30 минут)

Теперь загрузим полную программу. Вставь своё значение BPM, которое ты подобрал на предыдущем шаге:

```cpp
// ===================================
// Тактильный метроном для терапии болезни Паркинсона
// ===================================

#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

// ---------- НАСТРОЙКИ  ----------
const int MOTOR_PIN = 9;        // PWM пин для мотора (через резистор Gate 220Ω)
const int BUTTON_START = 2;     // Кнопка старт/стоп
const int POT_PIN = A0;         // Потенциометр для выбора темпа

// Эти параметры можно менять:
int DEFAULT_BPM = 90;           // начальный темп при старте
const int VIBRATION_DURATION = 100; // длительность вибрации
const int MIN_BPM = 40;         // минимальная частота
const int MAX_BPM = 180;        // максимальная частота
const int SMOOTHING = 4;        // сглаживание показаний потенциометра

// OLED
#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET -1
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

// ----------------------------------------------------------------

int currentBPM = DEFAULT_BPM;
bool isRunning = false;
unsigned long lastBeatTime = 0;

// Для обработки кнопки (антидребезг)
bool lastButtonState = HIGH;
unsigned long lastDebounceTime = 0;
const int DEBOUNCE_DELAY = 50;

// Для сглаживания показаний потенциометра
int potBuffer[SMOOTHING];
int potIndex = 0;

void setup() {
  Serial.begin(9600);
  Serial.println("=== Тактильный метроном ===");
  
  // Инициализация пинов
  pinMode(MOTOR_PIN, OUTPUT);
  digitalWrite(MOTOR_PIN, LOW); // Гарантированно выключаем мотор
  pinMode(BUTTON_START, INPUT_PULLUP);
  pinMode(POT_PIN, INPUT);

  // Инициализация дисплея
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) {
    Serial.println(F("OLED дисплей не найден!"));
    // Можно добавить мигание светодиодом как индикацию ошибки
  } else {
    Serial.println(F("OLED дисплей инициализирован"));
    display.clearDisplay();
    display.setTextSize(2);
    display.setTextColor(SSD1306_WHITE);
    display.setCursor(10, 20);
    display.println("TACTILE");
    display.setCursor(10, 40);
    display.println("METRONOME");
    display.display();
    delay(2000);
  }

  // Короткая вибрация при включении (двойная)
  digitalWrite(MOTOR_PIN, HIGH);
  delay(100);
  digitalWrite(MOTOR_PIN, LOW);
  delay(100);
  digitalWrite(MOTOR_PIN, HIGH);
  delay(100);
  digitalWrite(MOTOR_PIN, LOW);

  // Инициализация буфера потенциометра
  for (int i = 0; i < SMOOTHING; i++) {
    potBuffer[i] = analogRead(POT_PIN);
  }

  // Первое обновление дисплея
  updateDisplay();
}

void updateDisplay() {
  if (!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) return; // защита
  
  display.clearDisplay();
  
  // Заголовок
  display.setTextSize(1);
  display.setCursor(0, 0);
  display.println("TACTILE METRONOME");
  display.drawLine(0, 10, 128, 10, SSD1306_WHITE);

  // Основной BPM
  display.setTextSize(3);
  display.setCursor(10, 20);
  display.print(currentBPM);

  // Единицы измерения
  display.setTextSize(1);
  display.setCursor(90, 30);
  display.print("BPM");

  // Полоса прогресса темпа
  int progressWidth = map(currentBPM, MIN_BPM, MAX_BPM, 0, 118);
  display.drawRect(5, 45, 118, 8, SSD1306_WHITE);
  display.fillRect(5, 45, progressWidth, 8, SSD1306_WHITE);

  // Статус
  display.setCursor(0, 55);
  if (isRunning) {
    display.print("Status: RUNNING");
  } else {
    display.print("Status: STOPPED");
  }

  display.display();
}

int readSmoothedPot() {
  potBuffer[potIndex] = analogRead(POT_PIN);
  potIndex = (potIndex + 1) % SMOOTHING;
  
  long sum = 0;
  for (int i = 0; i < SMOOTHING; i++) {
    sum += potBuffer[i];
  }
  return sum / SMOOTHING;
}

void loop() {
  unsigned long currentTime = millis();

  // === ОБРАБОТКА КНОПКИ С АНТИДРЕБЕЗГОМ ===
  bool reading = digitalRead(BUTTON_START);
  
  if (reading != lastButtonState) {
    lastDebounceTime = currentTime;
  }
  
  if ((currentTime - lastDebounceTime) > DEBOUNCE_DELAY) {
    if (reading == LOW && lastButtonState == HIGH) {
      // Кнопка нажата!
      isRunning = !isRunning;
      
      if (isRunning) {
        Serial.println("Метроном ЗАПУЩЕН");
        // Подтверждение - двойная короткая вибрация
        digitalWrite(MOTOR_PIN, HIGH);
        delay(100);
        digitalWrite(MOTOR_PIN, LOW);
        delay(100);
        digitalWrite(MOTOR_PIN, HIGH);
        delay(100);
        digitalWrite(MOTOR_PIN, LOW);
        lastBeatTime = currentTime;
      } else {
        Serial.println("Метроном ОСТАНОВЛЕН");
        // Длинная вибрация при остановке
        digitalWrite(MOTOR_PIN, HIGH);
        delay(300);
        digitalWrite(MOTOR_PIN, LOW);
      }
      
      updateDisplay();
    }
  }
  
  lastButtonState = reading;

  // === ОБРАБОТКА ПОТЕНЦИОМЕТРА ===
  int potVal = readSmoothedPot();
  int mappedBPM = map(potVal, 0, 1023, MIN_BPM, MAX_BPM);
  
  // Плавное изменение BPM (фильтрация резких скачков)
  if (abs(mappedBPM - currentBPM) >= 1) {
    currentBPM = mappedBPM;
    
    // Если метроном запущен - плавное изменение темпа
    if (isRunning) {
      // Короткий сигнал при изменении темпа на ходу
      digitalWrite(MOTOR_PIN, HIGH);
      delay(20);
      digitalWrite(MOTOR_PIN, LOW);
    }
    
    updateDisplay();
    
    // Вывод в Serial для отладки
    Serial.print("BPM изменен на: ");
    Serial.println(currentBPM);
  }

  // === ГЕНЕРАЦИЯ РИТМА ===
  if (isRunning) {
    unsigned long interval = 60000 / currentBPM; // период в миллисекундах
    
    if (currentTime - lastBeatTime >= interval) {
      // Включаем мотор на заданную длительность
      digitalWrite(MOTOR_PIN, HIGH);
      delay(VIBRATION_DURATION);
      digitalWrite(MOTOR_PIN, LOW);
      
      lastBeatTime = currentTime;
      
      // Мигание индикатора на экране (опционально)
      static bool blinkState = false;
      blinkState = !blinkState;
    }
  }

  // Небольшая задержка для стабильности
  delay(10);
}
```

**Как работают кнопки:**

- **СТАРТ/СТОП** — включает и выключает метроном (двойная вибрация при старте, длинная при остановке)
- **БЫСТРЕЕ** / **МЕДЛЕННЕЕ** — нужно крутить потенциометр


---

### 🏗️ Этап 7: Финальная Сборка и Тестирование

**Цель:** Превратить нашу электронную схему в удобное и надёжное носимой устройство.

#### 🔧 Сборка корпуса (30 минут)

1. **Подготовка компонентов:**
    - Аккуратно собери все компоненты на небольшой макетной плате
    - Убедись, что все соединения прочные и не коротят
    - Закрепи MOSFET транзистор на небольшом радиаторе (можно вырезать из банки)

2. **Размещение в корпусе:**
    - Возьми пластиковую коробочку 5×3×2 см
    - Размести Arduino, плату и батарею внутри
    - **Вибромотор** приклей сбоку корпуса так, чтобы он плотно прилегал к коже при ношении
    - Закрепи OLED-дисплей на внутренней стороне крышки

3. **Крепление элементов управления:**
    - Выведи потенциометр через отверстие в корпусе
    - Закрепи кнопку START/STOP на удобном месте
    - Сделай отверстие для USB-зарядки модуля TP4056

#### ⚡ Финальное тестирование (20 минут)

**Проверь все функции устройства:**

✅ **Тест вибрации:**
- Надень браслет на ногу
- Включи устройство
- Почувствуй ритмичную вибрацию

✅ **Тест управления:**
- Покрути потенциометр - темп должен плавно меняться
- Нажми кнопку START/STOP - устройство должно включаться/выключаться
- Проверь отображение на OLED-экране

✅ **Тест автономности:**
- Отключи от USB
- Убедись, что устройство работает от батареи
- Проверь зарядку через TP4056 модуль

---
### 🎯 Практическое применение

**Как использовать готовое устройство:**

1. **Надень браслет** на запястье так, чтобы вибромотор плотно прилегал к коже
2. **Установи комфортный темп** с помощью потенциометра (обычно 80-100 BPM для ходьбы)
3. **Начни движение** синхронизируя шаги с ритмом вибрации
4. **Постепенно увеличивай темп** по мере улучшения походки

**Рекомендации по использованию:**

- Начинай с 10-15 минут в день
- Используй на ровной безопасной поверхности
- Сочетай с обычными упражнениями ЛФК
- Отслеживай прогресс в дневнике

---

## 🎉 Поздравляю, Юный Биомедицинский Инженер!

Ты только что самостоятельно спроектировал, запрограммировал и собрал настоящее медицинское устройство, которое может реально помочь людям с болезнью Паркинсона! Ты прошёл весь путь от идеи до готового продукта:

- 🔌 **Собрал электронную схему** с Arduino и внешними компонентами
- 💻 **Написал программу** с продвинутыми функциями (OLED-дисплей, сглаживание показаний, защита от дребезга)
- 🔋 **Организовал автономное питание** с возможностью зарядки
- 🏥 **Создал устройство** с реальной медицинской пользой