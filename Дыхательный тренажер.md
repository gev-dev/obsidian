# 🧘 Пособие для Юного Инженера: Создание «Дыхательного тренажера с тактильным и визуальным guidance»

---

## 🎯 Что мы будем делать?

Мы создадим умное устройство, которое поможет справляться со стрессом, тревогой и паническими атаками через правильное, диафрагмальное дыхание. Это не просто гаджет, а персональный тренер по дыханию.

Наш **Дыхательный тренажер** будет:

- **Чувствовать твое дыхание:** С помощью датчика на поясе он будет точно отслеживать, как ты дышишь.
    
- **Показывать ритм:** На красивом экране или светодиодной шкале будет отображаться анимация вдоха и выдоха.
    
- **Направлять тактильно:** Ты будешь _чувствовать_ ритм через мягкую вибрацию, которая подскажет, когда делать вдох, а когда выдох. Это особенно полезно, если ты не можешь смотреть на экран.
    
- **Следовать терапевтическому паттерну:** Устройство будет направлять тебя по проверенному ритму (например, 4 секунды вдох, 6 секунд выдох), который успокаивает нервную систему.
    

---

## 🛠️ Что нам понадобится?

### Электронные компоненты:

1. **Микроконтроллер:** Arduino Nano / Uno / Pro Mini (мозг устройства).
    
2. **Датчик дыхания (на выбор):**
    
    - **Датчик растяжения (Stretch Sensor)** - лучший вариант, точно измеряет расширение грудной клетки/живота.
        
    - **Тензорезистор (FSR - Force Sensing Resistor)** - можно использовать для измерения давления пояса.
        
    - **IMU-датчик (например, MPU-6050)** - с акселерометром и гироскопом, может отслеживать движение.
        
3. **Дисплей (на выбор):**
    
    - **OLED-дисплей 0.96" I2C** - компактный, четкий, идеален для анимации.
        
    - **Светодиодная линейка (например, из 10 светодиодов или адресная LED-лента WS2812B)** - для простого и наглядного отображения.
        
4. **Вибромотор:** Как в мобильных телефонах или игровых контроллерах.
    
5. **Резисторы:** На 10 кОм (для датчика растяжения), 220 Ом (для светодиодов).
    
6. **Провода-перемычки** и макетная плата.
    
7. **Аккумулятор Li-Po 3.7V** и модуль зарядки TP4056 для автономной работы.
    

### Материалы для сборки:

- **Пояс или ремешок:** Эластичный браслет, пояс для фитнеса или просто широкий кусок эластичной ткани.
    
- **Корпус для электроники:** Небольшая пластиковая коробочка или напечатанный на 3D-принтере корпус.
    
- **Клей, липучка, нитки** для крепления датчика и электроники к поясу.
    

### Программное обеспечение:

- **Arduino IDE (с установленными библиотеками):**
    
    - `Adafruit SSD1306` и `Adafruit GFX` (для OLED-дисплея).
        
    - `FastLED` (если используете адресную LED-ленту).
        

---

## 🤔 Как это работает?

### Научное обоснование:

**Диафрагмальное дыхание** активирует парасимпатическую нервную систему, которая отвечает за расслабление и восстановление ("режим отдыха"). Это:

- Снижает частоту сердечных сокращений.
    
- Понижает кровяное давление.
    
- Уменьшает уровень гормонов стресса (кортизола).
    
- Помогает выйти из состояния "бей или беги", характерного для панической атаки.
    

**Проблема:** В состоянии стресса сложно самостоятельно контролировать и замедлять дыхание.

**Решение нашего устройства:** Оно берет на себя функцию внешнего регулятора. **Визуальная** анимация и **тактильная** обратная связь:

1. Отвлекают мозг от тревожных мыслей.
    
2. Дают четкую, простую для выполнения задачу.
    
3. Синхронизируют физиологический ритм тела с заданным паттерном.
    

### Принцип работы устройства:

1. **Сенсорный вход:** Датчик на поясе измеряет изменение объема твоей грудной клетки или живота при дыхании.
    
2. **Обработка данных:** Arduino считывает показания датчика, фильтрует их от шума и определяет фазу дыхания (вдох/выдох).
    
3. **Генерация ритма:** Одновременно Arduino управляет встроенным "метрономом", который задает идеальный ритм дыхания (например, 4 сек вдох - 6 сек выдох).
    
4. **Обратная связь:**
    
    - **Визуальная:** На экране плавно растет и уменьшается шкала или круг, следуя целевому ритму.
        
    - **Тактильная:** Вибромотор дает короткие импульсы в ключевые моменты смены фаз (например, два коротких импульса для начала вдоха, один длинный для начала выдоха).
        

---

## 🔨 Пошаговая сборка

### Шаг 1: Подключение датчика дыхания (30 минут)

**Схема подключения датчика растяжения:**

Датчик растяжения работает как переменный резистор. Мы подключим его по схеме делителя напряжения.

text

ARDUINO          ->    ДАТЧИК И РЕЗИСТОР

   5V            ->    Один конец датчика растяжения
   A0            ->    Средняя точка (между датчиком и резистором 10кОм)
   GND           ->    Один конец резистора 10кОм

- Второй конец датчика растяжения подключается ко второму концу резистора 10кОм.
    

**Код для тестирования датчика:**

cpp

const int SENSOR_PIN = A0;  // Пин для датчика растяжения

void setup() {
  Serial.begin(9600);
}

void loop() {
  int sensorValue = analogRead(SENSOR_PIN);
  Serial.println(sensorValue); // Выводим значение в Serial Plotter

  // Надень пояс и подыши. Ты должен видеть четкие пики на графике.
  // При вдохе значение должно РАСТИ (если датчик растягивается).
  // При выдохе значение должно ПАДАТЬ.
  delay(50);
}

**Что делать:**

1. Загрузи код.
    
2. Открой **Инструменты -> Монитор порта**.
    
3. Надень пояс с датчиком и подыши.
    
4. Ты должен увидеть на графике волну, соответствующую твоему дыханию. Запомни диапазон значений при спокойном дыхании.
    

---

### Шаг 2: Подключение и тест дисплея (20 минут)

**Схема подключения OLED-дисплея (I2C):**

text

ARDUINO          ->    OLED DISPLAY

   A4 (SDA)      ->    SDA
   A5 (SCL)      ->    SCL
   5V            ->    VCC
   GND           ->    GND

**Код для тестирования дисплея и анимации дыхания:**

cpp

#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET -1
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

// Параметры дыхания
int breathDurationInhale = 4000; // 4 сек вдох
int breathDurationExhale = 6000; // 6 сек выдох
unsigned long previousBreathTime = 0;
bool isInhaling = true;

void setup() {
  Serial.begin(9600);
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) {
    Serial.println(F("OLED не найден!"));
    for(;;); // Бесконечный цикл при ошибке
  }
  display.clearDisplay();
  display.setTextSize(1);
  display.setTextColor(SSD1306_WHITE);
}

void loop() {
  unsigned long currentTime = millis();
  unsigned long cycleTime = isInhaling ? breathDurationInhale : breathDurationExhale;

  // Расчет прогресса дыхания (от 0 до 1)
  float progress = (float)(currentTime - previousBreathTime) / cycleTime;

  if (progress >= 1.0) {
    // Смена фазы
    isInhaling = !isInhaling;
    previousBreathTime = currentTime;
    progress = 0.0;
  }

  // Отрисовка анимации на дисплее
  display.clearDisplay();

  // Прогресс-бар вдоха/выдоха
  int barWidth = progress * SCREEN_WIDTH;
  display.fillRect(0, 0, barWidth, 10, SSD1306_WHITE);

  // Текстовая подсказка
  display.setCursor(0, 20);
  if (isInhaling) {
    display.print("INHALE");
  } else {
    display.print("EXHALE");
  }

  // Круг, который "дышит"
  int circleSize;
  if (isInhaling) {
    circleSize = 10 + (int)(progress * 30); // Растет на вдохе
  } else {
    circleSize = 40 - (int)(progress * 30); // Уменьшается на выдохе
  }
  display.drawCircle(SCREEN_WIDTH/2, 45, circleSize, SSD1306_WHITE);

  display.display();
  delay(50); // Небольшая задержка для стабильности
}

**Что должно получиться:** На экране будет отображаться растущий и уменьшающийся круг, полоса прогресса и текстовая подсказка, автоматически меняющаяся каждые 4 и 6 секунд.

---

### Шаг 3: Подключение и тест вибромотора (15 минут)

**Схема подключения (через транзистор для управления мощностью):**

text

ARDUINO          ->    ЦЕПЬ ВИБРОМОТОРА

   D9 (PWM)      ->    Резистор 220 Ом -> Затвор (Gate) MOSFET (IRF840)
   Виномотор (+) ->    Плюс питания (5V)
   Виномотор (-) ->    Сток (Drain) MOSFET
   Исток (Source) MOSFET -> GND
   Диод 1N4007 (катодом к + мотора, анодом к - мотора) ПАРАЛЛЕЛЬНО мотору.

_Примечание: Диод нужен для гашения обратных токов (индуктивных выбросов) от мотора._

**Код для тестирования вибрации по ритму:**

cpp

const int MOTOR_PIN = 9;

void setup() {
  pinMode(MOTOR_PIN, OUTPUT);
}

void loop() {
  // Имитация ритма: 2 коротких импульса -> пауза -> 1 длинный импульс
  // Сигнал "ВДОХ"
  digitalWrite(MOTOR_PIN, HIGH); delay(100);
  digitalWrite(MOTOR_PIN, LOW); delay(100);
  digitalWrite(MOTOR_PIN, HIGH); delay(100);
  digitalWrite(MOTOR_PIN, LOW);

  delay(3800); // Ждем оставшееся время вдоха (4 сек - 0.3 сек на вибрацию)

  // Сигнал "ВЫДОХ"
  digitalWrite(MOTOR_PIN, HIGH); delay(300);
  digitalWrite(MOTOR_PIN, LOW);

  delay(5700); // Ждем оставшееся время выдоха (6 сек - 0.3 сек)
}

**Что должно получиться:** Мотор будет давать две короткие вибрации, затем паузу, затем одну длинную вибрацию, и цикл повторится.

---

### Шаг 4: Объединение всей системы и финальный код (40 минут)

Теперь соберем все вместе. Логика финальной программы:

1. Вести отсчет времени для целевого ритма дыхания.
    
2. В ключевые моменты (начало вдоха/выдоха) запускать вибросигналы.
    
3. Обновлять анимацию на экране в реальном времени.
    
4. _Опционально:_ Сравнивать целевой ритм с реальными показаниями датчика и подстраивать guidance.
    

**Финальный код (базовая версия с guidance по таймеру):**

cpp

#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET -1
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

// Пины
const int MOTOR_PIN = 9;
const int SENSOR_PIN = A0;

// Параметры дыхательного ритма
const int INHALE_DURATION = 4000;  // Длительность вдоха (мс)
const int EXHALE_DURATION = 6000;  // Длительность выдоха (мс)
enum BreathPhase { INHALE, EXHALE, HOLD };
BreathPhase currentPhase = INHALE;
unsigned long phaseStartTime = 0;

void setup() {
  Serial.begin(9600);
  pinMode(MOTOR_PIN, OUTPUT);

  // Инициализация дисплея
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) {
    Serial.println(F("OLED не найден!"));
  }
  display.clearDisplay();
  display.setTextSize(1);
  display.setTextColor(SSD1306_WHITE);

  // Начальный вибросигнал и начало первого вдоха
  triggerVibration(2, 100); // Два коротких импульса для старта
  phaseStartTime = millis();
}

void loop() {
  unsigned long currentTime = millis();
  unsigned long phaseTime = currentTime - phaseStartTime;
  int sensorValue = analogRead(SENSOR_PIN); // Читаем датчик (можно использовать для усложненной логики)

  // Логика смены фаз и вибросигналов
  switch (currentPhase) {
    case INHALE:
      if (phaseTime >= INHALE_DURATION) {
        currentPhase = EXHALE;
        phaseStartTime = currentTime;
        triggerVibration(1, 300); // Один длинный импульс на выдох
      }
      break;
    case EXHALE:
      if (phaseTime >= EXHALE_DURATION) {
        currentPhase = INHALE;
        phaseStartTime = currentTime;
        triggerVibration(2, 100); // Два коротких импульса на вдох
      }
      break;
  }

  // Обновление дисплея
  updateDisplay(phaseTime, currentPhase);
  delay(50);
}

// Функция для управления вибрацией
void triggerVibration(int pulses, int duration) {
  for (int i = 0; i < pulses; i++) {
    digitalWrite(MOTOR_PIN, HIGH);
    delay(duration);
    digitalWrite(MOTOR_PIN, LOW);
    if (i < pulses - 1) delay(100); // Пауза между импульсами
  }
}

// Функция для обновления дисплея
void updateDisplay(unsigned long elapsed, BreathPhase phase) {
  display.clearDisplay();
  float progress;
  int circleSize;

  if (phase == INHALE) {
    progress = (float)elapsed / INHALE_DURATION;
    circleSize = 10 + (int)(progress * 30);
    display.setCursor(0, 0);
    display.print("INHALE");
  } else {
    progress = (float)elapsed / EXHALE_DURATION;
    circleSize = 40 - (int)(progress * 30);
    display.setCursor(0, 0);
    display.print("EXHALE");
  }

  // Рисуем "дыхающий" круг
  display.drawCircle(SCREEN_WIDTH/2, SCREEN_HEIGHT/2, circleSize, SSD1306_WHITE);

  // Рисуем прогресс-бар
  int barWidth = progress * (SCREEN_WIDTH - 10);
  display.drawRect(5, SCREEN_HEIGHT - 10, SCREEN_WIDTH - 10, 5, SSD1306_WHITE);
  display.fillRect(5, SCREEN_HEIGHT - 10, barWidth, 5, SSD1306_WHITE);

  display.display();
}

---

### Шаг 5: Сборка корпуса и финальные испытания (30 минут)

1. **Закрепи датчик на поясе:**
    
    - Аккуратно пришей или приклей датчик растяжения к эластичному поясу так, чтобы он располагался на животе или груди и растягивался при вдохе.
        
    - Убедись, что провода не натянуты и не мешают движению.
        
2. **Собери электронный модуль:**
    
    - Помести Arduino, аккумулятор и плату соединений в небольшую коробочку.
        
    - Закрепи вибромотор внутри коробочки так, чтобы вибрация хорошо передавалась на тело, либо выведи его отдельно и закрепи на поясе.
        
    - Закрепи OLED-дисплей на крышке коробки.
        
3. **Финальное тестирование:**
    
    - Надень устройство.
        
    - Включи питание.
        
    - Расслабься и попробуй синхронизировать свое дыхание с ритмом вибрации и анимации.
        
    - Поэкспериментируй с длительностью вдоха и выдоха в коде, чтобы найти самый комфортный для себя ритм.
        

---

## 🎉 Поздравляю, Юный Инженер-Бионик!

Ты только что создал не просто электронное устройство, а настоящий инструмент для ментального здоровья. Ты освоил работу с сенсорами, тактильной обратной связью и создал интуитивно понятный интерфейс. Этот проект отлично демонстрирует, как технологии могут быть не просто умными, но и по-настоящему полезными и заботливыми.

**Что можно улучшить в будущем:**

- **Добавить меню** для смены дыхательных паттернов (например, "Для сна", "Для концентрации").
    
- **Реализовать "адаптивный режим"**, где устройство будет подстраивать ритм под твое текущее дыхание.
    
- **Вести статистику** сеансов дыхания на SD-карте.
    
- **Сделать корпус** более эргономичным и стильным с помощью 3D-печати.
    

Теперь у тебя есть собственный персональный тренер по дыханию, который всегда под рукой!